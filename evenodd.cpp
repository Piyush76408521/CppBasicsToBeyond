#include <b its/stdc++.h>
using namespace std;
int main() {
int n;                              // Declaration of the value.
cin >> n;                           // Taking the number n.
if(n % 2 == 0) {                     // Condition for even number.
cout << "It is even number" << endl;
}
else {                                // If the number does not satisfy the condition of if statement then it print the else statement output.
cout << "It is odd number" << endl;
}
return 0;                           // Exists the program.
}
